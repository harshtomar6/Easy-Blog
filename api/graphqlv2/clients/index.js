// Client resources
const queries = require('./client.query');
const type = require('./client.type');
const mutation = require('./client.mutation');

module.exports = {
  queries,
  type,
  mutation
}