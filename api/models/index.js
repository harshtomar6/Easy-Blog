// Dependencies
const Clients = require('./clientsSchema');
const Admin = require('./adminSchema');
const Post = require('./postsSchema');
const Author = require('./authorSchema');

module.exports = {
  Clients,
  Admin,
  Post,
  Author
}