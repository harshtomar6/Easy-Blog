const postQueries = require('./postQuery');
const clientQueries = require('./clientQuery');
const authorQueries = require('./authorQuery')

module.exports = {
  postQueries,
  clientQueries,
  authorQueries
}